<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#004AAD"/> <!-- Fill color -->
    <!-- Optional: Specify corners for rounded rectangle -->
    <!-- Optional: Add a border -->
    <!-- Optional: Add size (you can also control the size with layout params) -->
    <gradient android:type="linear" android:startColor="#004AAD" android:endColor="#19A3FF"></gradient>
    <size android:width="200dp" android:height="100dp"/>
</shape>